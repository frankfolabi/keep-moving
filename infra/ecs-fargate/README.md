# Creating the cloud infrastructure on ECS

Ensure you have Terraform installed and properly configured to deploy the cloud resources.


Run:
```
terraform init
terraform plan
terraform apply -auto-approve
```
